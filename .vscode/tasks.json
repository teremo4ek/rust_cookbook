{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo run current binary",
            "type": "shell",
            "command": "cargo",
            "args": [
                "run",
                "--bin",
                "${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
